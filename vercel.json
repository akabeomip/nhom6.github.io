{
    "builds": [{
        "src": "newdjangoproject4/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "newdjangoproject4/wsgi.py"
        }
    ]
}